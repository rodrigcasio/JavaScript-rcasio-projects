// This is an example of using do..While statements

const cupsOfSugarNeeded = 7;
let cupsAdded = 0;

do {
    cupsAdded++;
    console.log(`${cupsAdded}. A cup of sugar was added.`);
}while(cupsAdded < cupsOfSugarNeeded);

