// Example to demonstrate a task being repeated

let vacationSpots = ['The vatican', 'The top of the Empire State', 'Athens'];

// loggin into the console, each place
console.log(vacationSpots[0]);
console.log(vacationSpots[1]);
console.log(vacationSpots[2]);

