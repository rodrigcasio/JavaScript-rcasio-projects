// Example of a forEach method

const fruits = ['mango', 'papaya', 'pineapple', 'apple'];

fruits.forEach(fruit => {
    console.log(`I want to eat a ${fruit}`);
});

// callback function : fruit => { console.log(... )};       anonymous function

// the iterator : forEach(...) 


