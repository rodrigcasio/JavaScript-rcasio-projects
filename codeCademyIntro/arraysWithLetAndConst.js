
let condiments = ['Ketchup', 'Mustard', 'Soy Sauce', 'Sriracha'];

const utensils = ['Fork', 'Knife', 'Chopsticks', 'Spork'];

// 1 re-assign the first element of condiments array, and log the array into the console
condiments[0] = 'Mayo';
console.log(condiments);

// 2.re-assign the array 'condiments' to be a new array with only one string and log the results ( it is possible )
condiments = ['Mayo'];
console.log(condiments); 

// 3 reassign the last item from the 'utensil' array to Spoon, and log the entire array:
utensils[3] = 'Spoon';
console.log(utensils);







